$(function(){
    // alert("Notice 注意:\n测试系统中的数据不会影响量产系统，测试数据每天午夜自动被量产数据覆盖。\nThe change on test system will not effect on production syste. Data of test system will be replaced by production system every midnight.\n切换到测试系统后，若要切换回量产系统，需要重新登录。\n进入测试系统后，左下角系统状态将用橙色显示以区分量产系统。");

    // //console.log(config)
    // disconnectSQLserver();
    // config.fSQLserver = 1;
    // config.testmode = 1;
    // config.dbbak = config.serverconfig.database;
    // config.serverconfig.database = "TEST_"+config.dbbak;
    // config.serverconfig.options.database = "TEST_"+config.dbbak;
    // console.log(config)
    // fs.writeFileSync(configFile, ini.stringify(config));
    // updateSQLserver();
    // connectSQLserver(function(){
    //     loadPanel($("div[bid=sidebar] a[perm]:visible:first-child").attr("href"));
    // });
    
})